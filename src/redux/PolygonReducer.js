const initialState = {}

function PolygonReducer(state = initialState, action) {
    switch(action.type) {
        case "":
            return;
        default:
            return state;
    }
}


export default PolygonReducer;